var elId       = '<%= @user.dom_id %>';
var el         = document.getElementById(elId);
var isExpanded = el.classList.contains('show');
var restoreEl  = document.createElement('div');
restoreEl.id   = elId;

if (isExpanded)
  restoreEl.classList.add('show');

el.replaceWith(restoreEl);

var snackbar       = document.createElement('div');
snackbar.innerHTML = '<%= j render(Material::Snackbar.new(flash: flash, action: link_to(t('helpers.submit.undo'), restore_user_path(@user), method: :post, remote: true))) %>';
document.body.appendChild(snackbar.firstChild);
